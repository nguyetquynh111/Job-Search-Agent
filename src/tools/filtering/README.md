# Filtering Tool
This tool removes jobs that do not match the candidate's required preferences. It runs before scoring.

- See [INPUT.md](INPUT.md) for input fields.
- See [OUTPUT.md](OUTPUT.md) for output fields.
- Contract: `FilterJobsInput` to `FilterJobsOutput`.

The future implementation should be deterministic, keep job data unchanged, and explain every rejection.

