# Scoring Tool

This tool scores accepted jobs, ranks them, and selects up to three jobs for fit analysis.

- See [INPUT.md](INPUT.md) for input fields.
- See [OUTPUT.md](OUTPUT.md) for output fields.
- Contract: `ScoreJobsInput` to `ScoreJobsOutput`.

The future developer must define a clear scoring formula. The same input must always produce the same result.

